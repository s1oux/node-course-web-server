<div class="container moar-margin">

  <form action="/admin/add" method="get">
    <button class="btn btn-outline-dark" type="submit" name="button">
      Add new manager
    </button>
  </form>
  <p></p>
  {{#if managers.length}}
    <table class="table">
      <thead class="thead-dark">
        <tr>
          <th scope="col">email</th>
          <th scope="col"></th>
        </tr>
      </thead>
      <tbody>
        {{#each managers}}
        <tr>
          <td>{{this.email}}</td>
          <td>
            <form action="/admin/manager/{{this._id}}" method="get">
              <button class="btn btn-sm btn-outline-dark" type="submit" name="button">Details</button>
            </form>
            <!-- <div class="d-flex justify-content-between align-items-center">
              <div class="btn-group">
                <form action="/admin/edit/{{this.email}}" method="get">
                  <button class="btn btn-sm btn-secondary" type="submit" name="button">Edit</button>
                </form>

                <form action="/admin/remove/{{this.email}}" method="get">
                  <button class="btn btn-sm btn-outline-dark" type="submit" name="button">Remove</button>
                </form>

              </div>
            </div> -->

          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  {{else}}
    <p class="text-center">Have no managers yet.</p>
  {{/if}}

</div>
